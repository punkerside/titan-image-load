services:
  load:
    image: ${PROJECT}-${SERVICE}
    ports:
      - "8080:8080"
    deploy:
      resources:
        limits:
          cpus: 200m
          memory: 256M
        reservations:
          cpus: 200m
          memory: 256M